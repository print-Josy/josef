// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#55a2eb', // Change to your primary color
    },
    secondary: {
      main: '#c079b4', // Change to your secondary color
    },
  }, // 55a2eb
});

export default theme;
