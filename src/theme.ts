// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#c079b4', // Change to your primary color
    },
    secondary: {
      main: '#55a2eb', // Change to your secondary color
    },
  },
});

export default theme;
